import numpy as np

class Sphere:
    self.higher = 5.12
    self.lower = -5.12

    def evaluate(position):
        return np.sum(np.power(position, 2))

class Rosenbrock:
    self.higher = 5.12
    self.lower = -5.12

    def evaluate(position):
        return np.sum(np.power(position, 2))
